{
  "name": "json-diff-ts",
  "version": "1.0.0",
  "description": "A TypeScript port of LianjiaTech json-diff library for comparing two JSON strings",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "bun build ./src/index.ts --outdir ./dist --target node",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "keywords": ["json", "diff", "compare"],
  "license": "MIT",
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.0.0"
  }
}
